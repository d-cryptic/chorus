# Copy to backend.hcl (git-ignored) and fill in. Then: tofu init -backend-config=backend.hcl
bucket    = "REPLACE_WITH_YOUR_R2_STATE_BUCKET"
endpoints = { s3 = "https://REPLACE_WITH_CF_ACCOUNT_ID.r2.cloudflarestorage.com" }
